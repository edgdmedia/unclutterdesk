name: Deploy On Main

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  deploy-landing:
    name: Deploy Landing To Cloudflare Pages
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build landing
        run: pnpm --filter @unclutterdesk/landing build

      - name: Deploy landing
        working-directory: apps/landing
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CF_PAGES_PROJECT: ${{ secrets.CLOUDFLARE_PAGES_LANDING_PROJECT }}
        run: pnpm dlx wrangler pages deploy dist --project-name="$CF_PAGES_PROJECT"

  deploy-app:
    name: Deploy App To Cloudflare Pages
    runs-on: ubuntu-latest
    needs: deploy-landing
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.2

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build app
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_APP_URL: ${{ secrets.VITE_APP_URL }}
        run: pnpm --filter @unclutterdesk/app build

      - name: Deploy app
        working-directory: apps/app
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CF_PAGES_PROJECT: ${{ secrets.CLOUDFLARE_PAGES_APP_PROJECT }}
        run: pnpm dlx wrangler pages deploy dist --project-name="$CF_PAGES_PROJECT"

  deploy-api:
    name: Deploy API To Server
    runs-on: ubuntu-latest
    needs: deploy-app
    permissions:
      contents: read
    steps:
      - name: Deploy API over SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          SEED_DB: "false"
        with:
          host: ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          envs: SEED_DB
          script: |
            cd /home/unclutterdesk/app
            chmod +x deploy.sh
            ./deploy.sh
