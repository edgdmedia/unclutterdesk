`AppHeader` sits above every workspace screen.

```jsx
<AppHeader eyebrow="Practice" title="Dashboard">
  <Input muted readOnly mono height={44} … />
  <Button variant="primary" size="lg">Copy Booking Link</Button>
  <IconButton size={44} dot><BellIcon/></IconButton>
</AppHeader>
```
