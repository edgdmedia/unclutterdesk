`Toggle` for the practice-status switch and its mobile equivalent. Pair it with helper copy that states the consequence.

```jsx
<Toggle checked={active} onChange={setActive} />
```

Turning it off hides the public booking page; already-booked sessions are unaffected. Say so next to the control.
