`BottomNav` is the mobile app's four-tab bar: Today, Schedule, Clients, Brand.

```jsx
<BottomNav items={tabs} active={tab} onSelect={setTab} />
```

Pin it to the bottom of the 390×844 frame; it is glass, so content scrolls behind it.
